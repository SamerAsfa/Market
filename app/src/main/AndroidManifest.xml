<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blackapp.market">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Market"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.basket.BasketActivity"
            android:theme="@style/Theme.Market.NoActionBar"/>
        <activity android:name=".ui.user_profile.UserProfileActivity"
            android:theme="@style/Theme.Market.NoActionBar"/>
        <activity android:name=".ui.user_auth.signup.SignupActivity"
            android:theme="@style/Theme.Market.NoActionBar"/>
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Market.NoActionBar" />
        <activity
            android:name=".ui.user_auth.login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Market.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>